apiVersion: v1
kind: Service
metadata:
  name: {{ include "cvx-hello.fullname" . }}
  labels:
    {{- include "cvx-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cvx-hello.selectorLabels" . | nindent 4 }}
