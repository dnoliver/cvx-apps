apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cvx-hello.fullname" . }}-test-connection"
  labels:
    {{- include "cvx-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cvx-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
