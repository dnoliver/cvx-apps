apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cvx-nodered.fullname" . }}-test-connection"
  labels:
    {{- include "cvx-nodered.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cvx-nodered.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
